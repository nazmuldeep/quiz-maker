import React from 'react';

const Options = ({ quizOption, rightAns }) => {
    return (
        <div className='option'>
            <label htmlFor=""></label>
        </div>
    );
};

export default Options;